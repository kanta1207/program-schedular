{
    "version": 2,
    "buildCommand": "npm run build",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/main.js",

        "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
      }
    ],
    "outputDirectory": "dist"
  }
  