{
    "version": 2,
    "buildCommand": "npm run build",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "/",

        "methods": ["GET", "POST", "PUT", "DELETE", "PATCH"]
      }
    ],
    "outputDirectory": "dist"
  }
  